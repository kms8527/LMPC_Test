{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "ROS: Launch",
          "type": "ros",
          "request": "launch",
          "target": "${workspaceFolder}/src/launch/lmpc.launch", // <<< Configure path to your launch file
          "preLaunchTask": "make_debug" // <<< This is the task that will run before debugging staself.racecar_env.init_maprts
      }
  ]
}